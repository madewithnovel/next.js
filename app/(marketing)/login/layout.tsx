export const metadata = {
	title: 'Login with Novel',
};

export default function Layout ({ children }) {
	/**
	 * Add a layout used by your login screens here.
	 */
	return (
		<>
			{children}
		</>
	);
}
